pipeline {
	agent {
		node {
			label 'master'
		}
	}

	properties([parameters([[$class: 'ChoiceParameter', choiceType: 'PT_SINGLE_SELECT', description: '', filterLength: 1, filterable: false, name: 'Image', randomName: 'choice-parameter-8648659754233']])])


			 stages {
				 stage('Build a docker image') {
					 steps {
						 sh """
					echo "${params.Image}"
                    """
					 }
				 }
			 }

//	post {
//		success{
//			echo "======== Trigger Jenkinsfile.Test ========"
//			script{
//				build job: 'job name', parameters: [string(name: 'image_name', value: "avzini/tor-socks-proxy:${env.BUILD_NUMBER}")]
//			}
//		}
//		cleanup {
//			cleanWs()
//		}
//	}
}

